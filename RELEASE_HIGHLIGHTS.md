<!-- Edit before each release; contents become the "## What's new" section of the release notes. -->

- **Input-device changes during capture** — Settings > Audio keeps the input-device picker available while capture is running. Picking another device now stops the current capture and restarts on the selected device with the same cadence.
- **Console settings tab** — console network access status and revoke now live under Settings > Console instead of being mixed into Storage.
