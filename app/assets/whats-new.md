<!-- Edit before each release: the "what's new" note shown once after updating. Bullet lines become the note; delete this file (or leave it empty) for a build with nothing to announce. -->
# What's new
- **Restart to Update works again:** after an update downloads, the button now quits the app so macOS can apply the staged update.
- **Cleaner install notes:** generated release instructions no longer include the `xattr` quarantine fallback.
