<!-- Edit before each release: the "what's new" note shown once after updating. Bullet lines become the note; delete this file (or leave it empty) for a build with nothing to announce. -->
# What's new
- **Input-device changes during capture:** the Settings > Audio input picker stays available while capture is running. Choosing a different device now restarts capture on that device.
- **Console has its own Settings tab:** console network access status and revoke moved out of Storage and into Settings > Console.
