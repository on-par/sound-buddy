import { describe, it } from 'vitest'

// TDD: tests written first, implementation follows via GitHub issues
describe('parseScene', () => {
  it.todo('parses scene name from header line')
  it.todo('parses channel config blocks')
  it.todo('parses mix on/off and fader level')
  it.todo('parses preamp gain')
  it.todo('parses EQ bands')
  it.todo('parses DCA states and levels')
})
