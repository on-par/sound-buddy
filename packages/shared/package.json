{
  "name": "@sound-buddy/shared",
  "version": "0.1.0",
  "license": "MIT",
  "description": "Cross-package DTOs shared among the Sound Buddy TypeScript workspace packages (scene-inspector, ai-analyst, audio-engine, cli).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --passWithNoTests --coverage"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
